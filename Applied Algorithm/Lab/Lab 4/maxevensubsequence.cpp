#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define MAX 1000000

int n;
int a[MAX+1];

void input() {
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> a[i];
}   

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);cout.tie(NULL);
    
    return 0;
}