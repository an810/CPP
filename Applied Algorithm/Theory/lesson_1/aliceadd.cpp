#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define el "\n"

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int t;
    cin >> t;
    for (int i=0; i<t; i++) {
        long long a, b;
        cin >> a >> b;
        cout << a + b << el;
    }
    return 0;
}